@model HnJ.Helper.Models.CheckboxViewModel

<div>
    @Html.CheckBoxFor(x => x.IsChecked)
    @Html.LabelFor(x => x.IsChecked, Model.Name)

    @Html.HiddenFor(x => x.Name)
    @Html.HiddenFor(x => x.Id)
</div>